<UserControl
    x:Class="TheXDS.Ganymede.Views.CustomDialogs.CustomTestDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:TheXDS.Ganymede.Views.CustomDialogs"
    xmlns:mcart="http://schemas.thexds.local/mcart"
    xmlns:vm="clr-namespace:TheXDS.Ganymede.ViewModels;assembly=TestApp"
    d:DataContext="{d:DesignInstance vm:CustomTestDialogViewModel}"
    mc:Ignorable="d" 
    Height="200" Width="400">
    <UniformGrid Rows="1" Columns="2">
        <Viewbox>
            <mcart:ProgressRing Value="{Binding RingValue}" Margin="10"/>
        </Viewbox>
        <Viewbox>
            <TextBlock Margin="20">
                Times ran: <Run Text="{Binding TimesRan}"/>
            </TextBlock>
        </Viewbox>
    </UniformGrid>
</UserControl>
