<TabItem
    x:Class="TheXDS.Ganymede.Pages.TabHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d"
    xmlns:vc ="clr-namespace:TheXDS.MCART.ValueConverters;assembly=MCART.WPF"
    xmlns:vm2="clr-namespace:TheXDS.Ganymede.ViewModels;assembly=Ganymede"    
    d:DataContext="{d:DesignInstance vm2:PageViewModel}"
    BorderBrush="{x:Null}">
    <TabItem.Resources>
        <vc:McartColorConverter x:Key="McartColorConverter"/>
    </TabItem.Resources>
    <TabItem.Header>
        <Border BorderThickness="0,0,0,1" BorderBrush="{Binding AccentColor, Converter={StaticResource McartColorConverter}}">
            <DockPanel>
                <Button 
                    Width="16"
                    VerticalAlignment="Center"
                    BorderThickness="0" 
                    Background="Transparent"
                    DockPanel.Dock="Right"
                    Command="{Binding CloseCommand, Mode=OneWay}">x</Button>
                <TextBlock Margin="0,0,10,0" VerticalAlignment="Center" Text="{Binding Title}"/>
            </DockPanel>
        </Border>
    </TabItem.Header>
</TabItem>