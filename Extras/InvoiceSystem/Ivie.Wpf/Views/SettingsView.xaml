<UserControl
    x:Class="TheXDS.Ganymede.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:gn="http://schemas.thexds.local/ganymede"
    xmlns:vm="clr-namespace:TheXDS.Ivie.ViewModels;assembly=Ivie"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance vm:SettingsViewModel}"
    d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <ItemsControl DockPanel.Dock="Left" ItemsSource="{Binding SettingsPages, Mode=OneWay}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding Text, Mode=OneWay}" Command="{Binding Command, Mode=OneWay}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>            
        </ItemsControl>
        <gn:NavigationViewModelHost      
            VisualResolver="{gn:ProteusCrudBuilder}"
            DialogService="{Binding ChildDialogService, Mode=OneWay}"
            Navigator="{Binding ChildNavService, Mode=OneWay}"/>
    </DockPanel>
</UserControl>
