<UserControl
    x:Class="TheXDS.Ganymede.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:gn="http://schemas.thexds.local/ganymede"    
    xmlns:viewmodels="clr-namespace:TheXDS.Ganymede.ViewModels;assembly=TestApp"
    d:DataContext="{d:DesignInstance Type=viewmodels:LoginViewModel}"
    mc:Ignorable="d" Background="#80000000"
    HorizontalAlignment="Center"
    VerticalAlignment="Center">
    <StackPanel Width="300" Margin="5">
        <TextBlock Margin="5 5 5 0" Text="Username" Foreground="White"/>
        <gn:TextBoxEx
            Margin="5"
            Label="Username"
            Icon="👤"
            Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Margin="5 5 5 0" Text="Password" Foreground="White"/>
        <PasswordBox Margin="5" PasswordChanged="PasswordBox_PasswordChanged"/>
        <Button Width="60" Margin="5" Command="{Binding LoginCommand, Mode=OneWay}" Content="Login"/>
    </StackPanel>
</UserControl>
